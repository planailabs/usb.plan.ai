---
title: What this is
description: A one-paragraph overview of the project.
---

usb.plan.ai is a static web app with first-class documentation. The app and the
docs are two Astro projects in one pnpm workspace; `pnpm build` builds the docs
first, copies them into `public/docs/`, then builds the app over the top so the
whole thing ships as one static `dist/`.

Replace this page with a real overview of the project.
